describe("Testing the Jest framework is configured properly in this application", () => {
  it("Should pass if Jest is configured correctly", () => {
    var sum = 1 + 2;
    expect(sum).toBe(3);
  });
});
